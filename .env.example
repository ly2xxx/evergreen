# GitLab Configuration
GITLAB_ACCESS_TOKEN=glpat-your-gitlab-personal-access-token
GITLAB_URL=https://gitlab.example.com

# Repositories to scan (comma-separated)
REPOSITORIES=group/project1,group/project2

# Optional: Labels for merge requests
LABELS=dependencies,docker,automated

# Optional: Assignees (comma-separated usernames)
ASSIGNEES=devops-team,platform-engineer

# Optional: Registry URLs (comma-separated)
REGISTRY_URLS=https://index.docker.io,https://registry.gitlab.com

# Optional: Logging level
LOG_LEVEL=INFO

# Optional: Cache configuration
CACHE_DIR=/tmp/evergreen-cache
CACHE_TTL=3600

# Optional: Schedule (cron format)
SCHEDULE=0 2 * * MON

# Optional: Create merge requests
CREATE_MRS=true

# Optional: Auto-merge (not implemented)
AUTOMERGE=false